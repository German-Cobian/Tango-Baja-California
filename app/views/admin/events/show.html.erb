<p style="color: green" class="ms-5"><%= notice %></p>

<div class="border border-danger rounded my-3 mx-5 py-5 px-5">
  <%= render @admin_event %>

  <div class="d-flex flex-row justify-content-end mb-3">
    <%= button_to "Elimina este Evento", admin_event_path(@admin_event.id), method: :delete, class: 'btn btn-outline-danger py-0' %>
  </div>

  <div>
    <div class="d-flex flex-row mb-3">
      <%= link_to "Edita este Evento", edit_admin_event_path(@admin_event), class: 'me-3' %> |
      <%= link_to "Listado de Eventos", admin_events_path,  class: 'ms-3' %>
    </div>
  </div>
  
</div>
